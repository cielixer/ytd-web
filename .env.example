# PIN for authentication (4 digits)
PIN=1234

# Server port
PORT=3000

# Rate limiting: max requests per minute per IP
RATE_LIMIT_MAX=5

# Session secret (change this to a random string)
SESSION_SECRET=change-me-to-a-random-string
